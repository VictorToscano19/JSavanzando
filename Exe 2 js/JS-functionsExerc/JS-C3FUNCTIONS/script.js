/* 
                Funciones
*/
        //Funcion que retorna y recibe parametros
function sumar (x, y){
                        return x + y;
}
let resultado = sumar(4, 8)
console.log(resultado);



//Calcular el cuadrado de un número
function calCuadrado (numero){
                        return numero * numero;
}
let resultCuadrado = calCuadrado (5);
console.log(resultCuadrado);



//              CALCULAR SEGUNDOS
function convertir (x){
                          return x * 60;
}
let resultadoSeg = convertir (5);
console.log(resultadoSeg);


                // SUMAR + 1
function add (x) {
                    return x + 1;
}
let resultadoAdd = add (3);
console.log(resultadoAdd);


                // AREA TRIANGULO
function area (x, y) {
                        return x*y/2;
}
let resultadoArea = area (7, 4);
console.log(resultadoArea);


                //AGE TO DAYS
function dias (z){
                    return z * 365;
}
let resultadoDias = dias (65);
console.log(resultadoDias);

                //POWER CALCULATOR
function OHM (x, y) {
                    return x*y;
} 
let resultadoPower = OHM (230, 10);
console.log(resultadoPower); 

                //RETURN SOMETHING
